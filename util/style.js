import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  dotted_line: {
    borderStyle: "dotted",
    borderWidth: 1,
    borderRadius: 2,
    borderColor: "#eff3f8",
  },
});
